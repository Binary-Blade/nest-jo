/**
 * Enumeration for user roles.
 * @enum {string}
 */
export enum UserRole {
  /**
   * Standard user role.
   * @example
   * UserRole.USER
   */
  USER = 'USER',

  /**
   * Admin user role.
   * @example
   * UserRole.ADMIN
   */
  ADMIN = 'ADMIN'
}
