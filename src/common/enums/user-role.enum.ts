/**
 * Enum for user role
 * @export UserRole
 * @enum {string}
 * @property {string} USER = 'USER'
 * @property {string} ADMIN = 'ADMIN'
 */
export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN'
}
