/**
 * Enum for sort order
 *
 * @export SortOrder
 * @enum {string}
 * @property {string} ASC = 'ASC'
 * @property {string} DESC = 'DESC'
 */
export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC'
}
