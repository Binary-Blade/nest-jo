/**
 * Enumeration for sort order options.
 * @enum {string}
 */
export enum SortOrder {
  /**
   * Ascending order.
   * @example
   * SortOrder.ASC
   */
  ASC = 'ASC',

  /**
   * Descending order.
   * @example
   * SortOrder.DESC
   */
  DESC = 'DESC'
}
